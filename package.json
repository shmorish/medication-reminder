{
  "name": "discord-notification",
  "version": "1.0.0",
  "description": "Discord notification for medication reminder",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "cron": "npm run build && node dist/cron.js",
    "dev": "ts-node src/cron.ts"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0"
  }
}